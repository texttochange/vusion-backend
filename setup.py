from setuptools import setup, find_packages

setup(
    name = "vusion-backend",
    version = "0.1",
    url = 'http://github.com/texttochange/vusion-backend',
    license = 'MIT',
    description = "Vusion backend for TextToChange app",
    author = 'Text To Change',
    packages = find_packages(),
    install_requires = [],
)

